{
  "$defs": {
    "AgentRequest": {
      "properties": {
        "id": {
          "$ref": "#/$defs/RequestId"
        },
        "method": {
          "type": "string"
        },
        "params": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "allOf": [
                    {
                      "$ref": "#/$defs/MessageRequest"
                    }
                  ],
                  "description": "Asks the peer to process a message.",
                  "title": "MessageRequest"
                }
              ],
              "description": "All possible requests that an agent can send to a client."
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": ["id", "method"],
      "type": "object",
      "x-docs-ignore": true
    },
    "AgentResponse": {
      "anyOf": [
        {
          "properties": {
            "id": {
              "$ref": "#/$defs/RequestId"
            },
            "result": {
              "anyOf": [
                {
                  "allOf": [
                    {
                      "$ref": "#/$defs/MessageResponse"
                    }
                  ],
                  "title": "MessageResponse"
                }
              ],
              "description": "All possible responses that an agent can send to a client."
            }
          },
          "required": ["id", "result"],
          "title": "Result",
          "type": "object"
        },
        {
          "properties": {
            "error": {
              "$ref": "#/$defs/Error"
            },
            "id": {
              "$ref": "#/$defs/RequestId"
            }
          },
          "required": ["id", "error"],
          "title": "Error",
          "type": "object"
        }
      ],
      "x-docs-ignore": true
    },
    "ClientRequest": {
      "properties": {
        "id": {
          "$ref": "#/$defs/RequestId"
        },
        "method": {
          "type": "string"
        },
        "params": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "allOf": [
                    {
                      "$ref": "#/$defs/MessageRequest"
                    }
                  ],
                  "description": "Asks the peer to process a message.",
                  "title": "MessageRequest"
                }
              ],
              "description": "All possible requests that a client can send to an agent."
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": ["id", "method"],
      "type": "object",
      "x-docs-ignore": true
    },
    "ClientResponse": {
      "anyOf": [
        {
          "properties": {
            "id": {
              "$ref": "#/$defs/RequestId"
            },
            "result": {
              "anyOf": [
                {
                  "allOf": [
                    {
                      "$ref": "#/$defs/MessageResponse"
                    }
                  ],
                  "title": "MessageResponse"
                }
              ],
              "description": "All possible responses that a client can send to an agent."
            }
          },
          "required": ["id", "result"],
          "title": "Result",
          "type": "object"
        },
        {
          "properties": {
            "error": {
              "$ref": "#/$defs/Error"
            },
            "id": {
              "$ref": "#/$defs/RequestId"
            }
          },
          "required": ["id", "error"],
          "title": "Error",
          "type": "object"
        }
      ],
      "x-docs-ignore": true
    },
    "Error": {
      "description": "A JSON-RPC error response.",
      "properties": {
        "code": {
          "type": "integer"
        },
        "message": {
          "type": "string"
        }
      },
      "required": ["code", "message"],
      "type": "object"
    },
    "MessageRequest": {
      "description": "Parameters for processing a peer message.",
      "properties": {
        "text": {
          "description": "Message text.",
          "type": "string"
        }
      },
      "required": ["text"],
      "type": "object",
      "x-method": "mcp/message",
      "x-side": "both"
    },
    "MessageResponse": {
      "description": "Result from processing a peer message.",
      "properties": {
        "acknowledged": {
          "description": "Whether the peer accepted the message.",
          "type": "boolean"
        }
      },
      "required": ["acknowledged"],
      "type": "object",
      "x-method": "mcp/message",
      "x-side": "both"
    },
    "RequestId": {
      "description": "A JSON-RPC request identifier.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "anyOf": [
    {
      "anyOf": [
        {
          "allOf": [
            {
              "$ref": "#/$defs/AgentRequest"
            }
          ],
          "title": "Request"
        },
        {
          "allOf": [
            {
              "$ref": "#/$defs/AgentResponse"
            }
          ],
          "title": "Response"
        }
      ],
      "description": "A JSON-RPC message sent by the agent.",
      "properties": {
        "jsonrpc": {
          "enum": ["2.0"],
          "type": "string"
        }
      },
      "required": ["jsonrpc"],
      "title": "Agent",
      "type": "object"
    },
    {
      "anyOf": [
        {
          "allOf": [
            {
              "$ref": "#/$defs/ClientRequest"
            }
          ],
          "title": "Request"
        },
        {
          "allOf": [
            {
              "$ref": "#/$defs/ClientResponse"
            }
          ],
          "title": "Response"
        }
      ],
      "description": "A JSON-RPC message sent by the client.",
      "properties": {
        "jsonrpc": {
          "enum": ["2.0"],
          "type": "string"
        }
      },
      "required": ["jsonrpc"],
      "title": "Client",
      "type": "object"
    }
  ],
  "title": "Both Side Method Test Fixture"
}
